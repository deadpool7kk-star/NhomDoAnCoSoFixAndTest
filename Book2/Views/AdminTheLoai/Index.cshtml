@model IEnumerable<Book2.Models.TheLoai>

@{
    Layout = "_AdminLayout";
    ViewData["Title"] = "Quản lý thể loại";
}

<div class="d-flex justify-content-between align-items-center mb-4">
    <h2 class="fw-bold mb-0">Quản lý thể loại</h2>
    <a asp-action="Create" class="btn btn-primary shadow-sm rounded-pill px-4">
        <i class="bi bi-plus-circle me-1"></i> Thêm thể loại
    </a>
</div>

<div class="card border-0 shadow-sm" style="border-radius: 12px; overflow: hidden;">
    <div class="table-responsive">
        <table class="table table-hover align-middle mb-0">
            <thead class="table-light">
                <tr>
                    <th class="ps-4" width="80">ID</th>
                    <th width="250">Tên thể loại</th>
                    <th>Mô tả</th>
                    <th class="text-center" width="120">Thao tác</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td class="ps-4 fw-semibold text-muted">#@item.Id</td>
                        <td class="fw-bold text-dark">
                            <span class="d-inline-block text-truncate" style="max-width: 200px;">@item.TenTheLoai</span>
                        </td>
                        <td class="text-muted">
                            <span class="d-inline-block text-truncate" style="max-width: 400px;">
                                @(string.IsNullOrEmpty(item.MoTa) ? "Chưa có mô tả" : item.MoTa)
                            </span>
                        </td>
                        <td class="text-center">
                            <div class="btn-group shadow-sm">
                                <a asp-action="Edit" asp-route-id="@item.Id" class="btn btn-light btn-sm text-primary border" title="Sửa">
                                    <i class="bi bi-pencil-square"></i>
                                </a>
                                <form asp-action="Delete" method="post" class="d-inline" onsubmit="return confirm('Bạn có chắc muốn xóa thể loại này?');">
                                    @Html.AntiForgeryToken()
                                    <input type="hidden" name="id" value="@item.Id" />
                                    <button type="submit" class="btn btn-light btn-sm text-danger border" title="Xóa">
                                        <i class="bi bi-trash"></i>
                                    </button>
                                </form>
                            </div>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
    @if (!Model.Any())
    {
        <div class="text-center py-5 text-muted">
            <i class="bi bi-inbox fs-1"></i>
            <p class="mt-2">Chưa có dữ liệu thể loại.</p>
        </div>
    }
</div>